# VeilSupport Environment Configuration
# Copy this file to .env for local development
# DO NOT commit .env to version control

# Database Configuration
DATABASE_URL=postgres://user:pass@localhost/veilsupport

# Test Database Configuration (for running tests)
# For Docker Compose development, use:
TEST_DATABASE_URL=postgres://veiluser:veilpass@localhost:5432/veilsupport?sslmode=disable

# Security Configuration
# IMPORTANT: Change this in production!
JWT_SECRET=your-secret-key-change-this

# XMPP Server Configuration
# For testing, you can use a free XMPP server like:
# Option 1: Local XMPP server (if you set one up separately)
# XMPP_SERVER=localhost:5222
# Option 2: Public test server (create account first)
XMPP_SERVER=xmpp.jp:5222

# XMPP Admin Credentials
# Create an account on your chosen XMPP server and use those credentials
# The admin JID that will receive user messages
XMPP_ADMIN_JID=veilsupport@xmpp.jp
# XMPP admin password for authentication
XMPP_ADMIN_PASSWORD=MySecurePass123!

# Server Configuration
# Port for the HTTP server to listen on
PORT=8080